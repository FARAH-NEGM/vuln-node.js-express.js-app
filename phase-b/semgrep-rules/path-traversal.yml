rules:
  - id: custom-path-traversal
    languages:
      - javascript
    message: "User input used in file system path"
    severity: ERROR
    pattern: fs.readFile(path.join(__dirname, $P), $CB)
